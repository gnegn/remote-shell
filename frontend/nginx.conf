events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name remoteshellsoftcom.duckdns.org www.remoteshellsoftcom.duckdns.org;

        # Certbot HTTP challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Редірект на HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name remoteshellsoftcom.duckdns.org www.remoteshellsoftcom.duckdns.org;

        ssl_certificate /etc/letsencrypt/live/remoteshellsoftcom.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/remoteshellsoftcom.duckdns.org/privkey.pem;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri /index.html;
        }

        location /api/ {
            proxy_pass http://api:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
        }

        location /api/internal/ {
            allow 172.20.0.0/16;
            deny all;
            proxy_pass http://api:8000$request_uri;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
        }
    }
}
